{
  "_comment": "Committed on purpose: these are throwaway local-only values that match docker-compose.yml. Jwt:Secret is deliberately NOT here - supply it via `dotnet user-secrets set \"Jwt:Secret\" \"<32+ chars>\"` or the Jwt__Secret environment variable.",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Information",
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  },
  "ConnectionStrings": {
    "Default": "Host=localhost;Port=5432;Database=assignmenthub;Username=assignmenthub;Password=assignmenthub"
  },
  "Jwt": {
    "Issuer": "AssignmentHub",
    "Audience": "AssignmentHubClient",
    "AccessTokenMinutes": 60
  },
  "Cors": {
    "AllowedOrigins": [ "http://localhost:3000" ]
  }
}
