# Next.js only reads .env.local from this directory, so copy this file:
#   cp .env.local.example .env.local
# See .env.example at the repository root for the backend variables.

# Base URL of the ASP.NET Core API, as seen from the browser.
NEXT_PUBLIC_API_URL=http://localhost:5080
